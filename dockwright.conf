# dockwright global configuration
#
# Single source of truth for where dockwright base charts live.
# This file is:
# - used by the Makefile (via `include`)
# - sourced by /usr/local/bin/deployer.sh at runtime

CHARTS_DIR=/usr/local/share/dockwright/charts